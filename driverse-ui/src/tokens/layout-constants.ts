/**
 * @extracted-from
 *   A: Driverse_FE_Autocredit @ b96eda3 src/layouts/dashboard/config.ts
 *   B: Driverse_FE_Business   @ b96eda3 src/layouts/dashboard/config.ts
 * @status identical
 * @notes Byte-identical in both apps; lifted verbatim. These are shell dimensions the layouts and the
 *        multi-tabs strip both measure against, so they sit in the token layer rather than in a layout
 *        module — that keeps them importable without pulling a layout in.
 */

export const NAV_WIDTH = 260;
export const NAV_COLLAPSED_WIDTH = 80;
export const NAV_HORIZONTAL_HEIGHT = 48;

export const HEADER_HEIGHT = 64;
export const MULTI_TABS_HEIGHT = 32;
